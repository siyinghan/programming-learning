cho1-hello-spring：使用的IoC，由Spring创建对象

实现步骤：
1. 创建Maven项目
2. 加入Maven的依赖
   Spring的依赖，版本5.2.5版本
   JUnit依赖
3. 创建类（接口和它的实现类）
   和没有使用框架一样，就是普通的类。
4. 创建Spring需要使用的配置文件
   声明类的信息，这些类由Spring创建和管理
5. 测试Spring创建的